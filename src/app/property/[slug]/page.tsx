import Wrapper from '@/layouts/Wrapper';
import PropertyDetails from 'src/components/site/PropertyDetails';

//const Property = (slug: string) => {
const Property = () => {
  return (
    <Wrapper>
      <PropertyDetails />
    </Wrapper>
  );
};

export default Property;
